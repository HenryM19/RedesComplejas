services:
  - type: web
    name: templates-library-mcp
    env: python
    plan: free
    
    buildCommand: pip install -r config/requirements.txt
    startCommand: python config/mcp_server.py
    
    healthCheckPath: /health
    
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
